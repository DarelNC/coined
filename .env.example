# Optional. GitHub code search is only used as a fallback when Sourcegraph
# fails — and only when this is set at all (GitHub's code search endpoint
# has no anonymous access, so without a token this fallback is skipped
# entirely, not attempted). A classic PAT with no scopes is enough for
# public code search. See docs/features/variable-search.md.
GITHUB_TOKEN=
